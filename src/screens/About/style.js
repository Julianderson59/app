import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        padding: 30,
        alignItems: 'center',
    },
    info: {
        marginBottom: 20,
        fontSize: 18,
        textAlign: 'center'
    }
})

export default styles